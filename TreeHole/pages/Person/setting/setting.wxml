<form bindsubmit="doSubmit">
  <view class="changeName">
    <textarea name="content" maxlength="20" style="height: 2em" placeholder="填新昵称~" />
    <button type="primary" size="{{primarySize}}" formType="submit" disabled="{{disabledSubmitBtn}}">确认修改</button>
  </view>

</form>